#ifndef FUNCTION_H
#define FUNCTION_H
enum {
    MAX_PATH = 260,
    MAX_DOMAIN = 255 
};

int input(char string[MAX_PATH], char *symbol_one, char *symbol_two);
int check(char *string);
//int process();
//int output();


#endif
